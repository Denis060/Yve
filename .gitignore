# Secrets
.env
.env.local
.env.*.local
# .env.example is used locally as a personal key vault (real values that
# can only be retrieved once). Keep it OUT of git despite the name.
.env.example
# Claude Code machine-local settings — can contain approved commands with
# tokens baked in. Personal to this machine; never commit.
.claude/settings.local.json
# Note: mobile/lib/config/env.dart only holds compile-time defaults.
# Real keys should be passed via --dart-define-from-file at build time.
# See mobile/dart_defines.json.template.
mobile/dart_defines.json

# Flutter / Dart
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.packages
mobile/build/
mobile/.idea/
mobile/.vscode/
mobile/pubspec.lock
mobile/ios/Pods/
mobile/ios/.symlinks/
mobile/ios/Flutter/Flutter.framework
mobile/ios/Flutter/Generated.xcconfig
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/app/build/

# Supabase
supabase/.branches/
supabase/.temp/
supabase/.env

# OS
.DS_Store
Thumbs.db
